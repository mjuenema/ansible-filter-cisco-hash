---

- hosts: localhost
  remote_user: root
  roles:
    - ansible-filter-cisco-hash
  vars:
    user: user
    password: password
  tasks:
    - name: Cisco Hash 5
      template:
        src: ciscohash5.j2
        dst: /tmp/ciscohash5
